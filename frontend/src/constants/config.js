
export const serverURL = `http://127.0.0.1:8000`;

export const apiURL = `${serverURL}/api/`;
export const usersURL = `${apiURL}/users`;
export const productsURL = `${apiURL}/products`;
export const cartURL = `${apiURL}/carts`;